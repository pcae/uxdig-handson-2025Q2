import { cn } from '@/lib/utils'

const Header = () => {
  return (
    <header className={cn('fixed top-0 z-20 w-full bg-black px-5 text-white sm:px-10')}>
      Header
    </header>
  )
}

export default Header
